This is a demo of creating topology using Mininet.
Steps to run the program:
1) type sudo python <file_name.py> $1 $2
2) $1= no of hosts/per switch
3) $2= no of switches
4) type ping all to see the result
